<snippet>
    <content><![CDATA[
@classmethod
def copy(cls, ${1:records}, default=None):
    if default is None:
        default = {}
    else:
        default = default.copy()
    ${3}
    return super(${2:ClassName}, cls).copy($1, default=default)

]]></content>
    <tabTrigger>tcopy</tabTrigger>
    <scope>source.python</scope>
    <description>Tryton: copy() reimplementation</description>
</snippet>
